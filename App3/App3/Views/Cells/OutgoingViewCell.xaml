<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="App3.Views.Cells.OutgoingViewCell">
    <Frame CornerRadius="20" IsClippedToBounds="True" Padding="10"
              Margin="80,10,10,10" BackgroundColor="#18648C">
    <StackLayout 
               IsVisible="True"   >
            <Label FontFamily="OpenSans-SemiBold"  HorizontalOptions="StartAndExpand" VerticalOptions="EndAndExpand" TextColor="White" Text="{Binding Descmsg}" FontSize="17"/>



            <Label Margin="0,-10,0,0" FontFamily="OpenSans-SemiBold" HorizontalOptions="EndAndExpand" VerticalOptions="End" TextColor="White" Text="{Binding Dtmsg, StringFormat='{}{0: dd/MM/yyyy à\\s HH:mm   }'}" FontSize="10"/>
       
           
    </StackLayout>
    </Frame>
</ViewCell>